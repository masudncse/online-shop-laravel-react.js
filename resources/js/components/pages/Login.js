import React from 'react';
import { AuthComponent } from '../components';

const Login = () => {
    return (
        <React.Fragment>
             <AuthComponent />
        </React.Fragment>
    );
};

export default Login;